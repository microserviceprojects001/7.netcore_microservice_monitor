# 打包镜像

sln 目录执行打包
docker build -f Project/Project.API/Dockerfile -t registry.cn-hangzhou.aliyuncs.com/microsoftservice/project.api:v1 .

# 临时修改开发环境配置文件

```
"ConnectionStrings": {
    "MySQLProject": "Server=localhost;port=3308;Database=beta_project;User=abel;Password=acsdev312!@;"
  },
```

D:\Code\1.microservice\2.netcore_microservice_userService\Project\Project.API

dotnet ef database update --startup-project . --project ../Project.Infrastructure
手动把数据库更新进去

# 启动 project-api 容器

D:\Code\1.microservice\2.netcore_microservice_userService\DeployApps>docker-compose up -d project-api

# contact-api 打包

docker build -f Contact.API/Dockerfile -t registry.cn-hangzhou.aliyuncs.com/microsoftservice/contact.api:v1 .

# 启动 contact-api 容器

# 启动命令

docker-compose up -d contact-api
docker-compose up -d recommend-api
docker-compose up -d project-api
docker-compose up -d user-api
