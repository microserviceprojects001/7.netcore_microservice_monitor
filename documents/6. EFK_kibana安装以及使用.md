# 安装 kibana

https://github.com/AbelLiuKai/docker_image_pusher/blob/main/images.txt
在这里添加，下载镜像：
docker.elastic.co/kibana/kibana:8.11.0

docker pull registry.cn-hangzhou.aliyuncs.com/abelcontainer/kibana:8.11.0
docker-compose.yml 添加如下

```

  # Kibana - 数据可视化平台
  kibana:
    image: registry.cn-hangzhou.aliyuncs.com/abelcontainer/kibana:8.11.0
    container_name: kibana
    environment:
      - ELASTICSEARCH_HOSTS=http://es-master:9200 # 核心：通过容器名连接
      # 如果未来开启安全认证，需添加如下配置（当前未开启）：
      # - ELASTICSEARCH_USERNAME=kibana_system
      # - ELASTICSEARCH_PASSWORD=your_password
      - XPACK_SECURITY_ENABLED=false # 与ES配置保持一致
    ports:
      - "5601:5601"
    networks:
      - elastic
    depends_on:
      - es-master
      - es-data1
      - es-data2
    mem_limit: 800m
    # 可选：增加健康检查，确保Kibana在ES完全就绪后才启动
    healthcheck:
      test: ["CMD", "curl", "-f", "http://es-master:9200"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s


```

我的方法，容器都在 elastic 网络中了
docker network ls # 可以做一些查询

# 可访问：

需要等一会儿
http://localhost:5601/app/home#/

# 老师的操作

https://www.elastic.co/docs/deploy-manage/deploy/self-managed/install-kibana-with-docker

docker network ls
docker network create search
docker network connect search elasticsearch # docker network connect 网络名字 容器名字
docker network inspect search # 查询网络中的 container
docker run -d --name kibana --net search -p 5601:5601 kibana:6.2.4

# kibana 使用

现在的状态是，通过之前的操作用 postman 操作，Elastic Search 中已经有 index 数据了，

## 查看 index

![alt text](<../截图/6. Kibana 找到系统中已经有的index.png>)

## 新建 data view

![alt text](<../截图/7. 需要新建data view.png>)

![alt text](<../截图/8. 创建data view.png>)

![alt text](<../截图/9. 应用 data view.png>)

![alt text](<../截图/10. 查看 log 导出report.png>)
